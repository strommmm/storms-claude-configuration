# 跨服务依赖关系文档

> 版本：v1.0
> 最后更新：{{DATE}}

<!--
  本文档仅适用于多服务项目。记录服务间通过网络协议（HTTP/gRPC/MQ）的依赖关系。
  模块内部依赖请参见各服务的 module-dependencies.md。
-->

---

## 1. 服务清单

| 服务标识 | 服务名称 | 职责描述 | 技术栈 | 负责人 |
|----------|----------|----------|--------|--------|
| | | | | |

---

## 2. 跨服务依赖总览

<!-- 文本形式的服务间依赖关系图 -->

```
服务A → 服务B     （HTTP: /api/v1/resource）
服务A → 服务C     （MQ: event.name）
服务B → 服务D     （gRPC: ServiceName.Method）
```

**依赖方向校验**：确认无循环依赖。

---

## 3. 同步调用依赖（HTTP / RPC）

| 调用方服务 | 被调用方服务 | 协议 | 接口路径/方法 | 用途 | 超时设置 | 失败策略 |
|-----------|-------------|------|--------------|------|----------|----------|
| | | HTTP/gRPC | | | | 重试/熔断/降级 |

---

## 4. 异步消息依赖（MQ / Event Bus）

| 消息/事件名称 | 发布方服务 | 消费方服务 | 通道类型 | 消息格式 | 幂等处理 |
|--------------|-----------|-----------|----------|----------|----------|
| | | | MQ/EventBus | | 是/否 — 描述 |

---

## 5. 数据共享依赖

<!-- 记录服务间共享的数据资源（如共享数据库、缓存、文件存储等） -->

| 共享资源 | 资源类型 | 所有者服务 | 消费者服务 | 访问方式 |
|----------|----------|-----------|-----------|----------|
| | DB/Cache/Storage | | | 直接访问/API 代理 |

---

## 6. 依赖规则

1. **禁止直接数据库访问**：服务间禁止直接访问对方的数据库，必须通过 API 或消息通信
2. **同步调用需超时熔断**：所有跨服务同步调用必须配置超时时间和熔断策略
3. **异步消息需幂等处理**：消息消费方必须实现幂等处理，防止重复消费
4. **服务间禁止代码引用**：不同服务不可直接 import 对方代码，仅通过网络协议通信

