# 项目拓扑与路径映射

> 拓扑类型：{{TOPOLOGY}}（单服务 / 多服务）
> 最后更新：{{DATE}}

<!--
  本文件为项目级配置，在主分支维护，所有研发分支共享。
  单服务项目：拓扑类型设为"单服务"，无需填写路径映射表。所有路径使用默认值。
  多服务项目：拓扑类型设为"多服务"，必须填写路径映射表。
-->

---

## 路径映射表（仅多服务项目需要）

| 服务标识 | 服务名称 | 服务根路径 | 模块路径模式 | 服务文档路径 | 说明 |
|----------|----------|-----------|-------------|-------------|------|
| | | | | | |

---

## 路径解析规则

1. **单服务项目**（默认）：使用本规范中的默认路径（`src/<module>/`、`docs/`）
2. **多服务项目**：将 `src/<module>/` 替换为映射表中对应服务的「模块路径模式」
3. **全局文档**（`docs/PRD.md`、`docs/topology.md` 等）始终在项目根 `docs/` 下
4. **每个服务**有独立的 `architecture.md` 和 `module-dependencies.md`，位于「服务文档路径」下
5. **跨服务依赖**记录在项目根 `docs/service-dependencies.md`
